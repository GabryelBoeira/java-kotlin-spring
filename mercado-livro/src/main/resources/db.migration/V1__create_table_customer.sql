#Criando a tabela de cliente
CREATE TABLE CUSTOMER (
    id BIGINT NOT NULL AUTO_INCREMENT GENERATED BY DEFAULT AS IDENTITY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
);

#Comentando as colunas
ALTER TABLE CUSTOMER MODIFY COLUMN id COMMENT 'Id da tabela de cliente';
ALTER TABLE CUSTOMER MODIFY COLUMN name COMMENT 'Nome do cliente';
ALTER TABLE CUSTOMER MODIFY COLUMN email COMMENT 'Email do cliente';
